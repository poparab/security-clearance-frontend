# Security Clearance Repository

Runtime app lives in `app/`.

Keep runtime HTML, admin UI, styles, scripts, assets, and related frontend resources under `app/`.
Keep docs, tools, data, and repository configuration at the repository root.