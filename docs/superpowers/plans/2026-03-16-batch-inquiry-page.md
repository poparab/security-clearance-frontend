# Batch Inquiry Page — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build `app/batch-inquiry.html` — a 4-step wizard for agency representatives to submit batch security inquiries, from downloading the Excel template through wallet deduction confirmation — plus a shared CSS design system that all portal pages reference.

**Architecture:** Extract all CSS from `app/index.html` into SCSS partials compiled to a single `app/styles/main.css`. The batch inquiry page is a single HTML file with inline JS that manages wizard step state. The Excel template is a 21-column `.xlsx` file generated by a Python script.

**Tech Stack:** Vanilla HTML/CSS/JS, Tajawal (Google Fonts), Font Awesome 6.4 CDN, openpyxl (Python, for xlsx template generation). No build tools required — `main.css` is pre-written (not compiled at runtime).

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `app/styles/_tokens.scss` | Create | CSS custom properties — colors, spacing, radius, shadows, typography |
| `app/styles/_reset.scss` | Create | Box-sizing, margin/padding reset, body defaults |
| `app/styles/_layout.scss` | Create | Top header, nav, breadcrumb bar, page hero, page body wrapper, footer |
| `app/styles/_components.scss` | Create | Cards, stats, pills, badges, tables, alerts, action cards, dropdowns, drawers |
| `app/styles/_wizard.scss` | Create | Stepper bar, step nodes, step lines, step panel show/hide, all step states |
| `app/styles/main.scss` | Create | Entry point — `@use` imports for all partials in order |
| `app/styles/main.css` | Create | Pre-compiled output — all CSS from all partials in one file, browser-ready |
| `app/index.html` | Modify | Remove `<style>` block (lines 11–468), add `<link>` to `main.css` |
| `app/registration.html` | Modify | Remove inline `<style>` block, add `<link>` to `main.css` |
| `app/batch-inquiry.html` | Create | Full wizard page — 4 step panels + JS state manager |
| `app/assets/batch-template.xlsx` | Create | 21-column Excel template with guidance row (via Python) |
| `tools/scripts/generate-batch-template.py` | Create | Python script that produces `batch-template.xlsx` using openpyxl |

---

## Chunk 1: Design System (SCSS Partials + main.css)

### Task 1: Create `app/styles/_tokens.scss`

**File:** `app/styles/_tokens.scss` (create)

- [ ] Use the existing `app/styles/` directory
- [ ] Write `_tokens.scss` with all design tokens extracted verbatim from `app/index.html`:

```scss
// ── Brand ──────────────────────────────────────────────────────────────
$color-primary:         #1d4ed8;
$color-primary-dark:    #1e3a8a;
$color-primary-mid:     #1a3a8f;
$color-primary-light:   #eff6ff;
$color-primary-muted:   #bfdbfe;
$color-primary-subtle:  #dbeafe;

// ── Semantic ────────────────────────────────────────────────────────────
$color-success:         #16a34a;
$color-success-dark:    #15803d;
$color-success-light:   #dcfce7;
$color-success-bg:      #f0fdf4;
$color-success-border:  #bbf7d0;

$color-warning:         #d97706;
$color-warning-dark:    #b45309;
$color-warning-deep:    #92400e;
$color-warning-light:   #fef3c7;
$color-warning-bg:      #fffbeb;
$color-warning-border:  #fde68a;

$color-danger:          #dc2626;
$color-danger-dark:     #b91c1c;
$color-danger-light:    #fee2e2;
$color-danger-bg:       #fff5f5;
$color-danger-border:   #fecaca;

$color-info-light:      #e0f2fe;

// ── Neutrals ────────────────────────────────────────────────────────────
$color-text-primary:    #0f172a;
$color-text-body:       #1e293b;
$color-text-secondary:  #475569;
$color-text-muted:      #64748b;
$color-text-placeholder:#94a3b8;
$color-text-subtle:     #cbd5e1;

$color-bg-page:         #f1f5f9;
$color-bg-alt:          #f8fafc;
$color-bg-card:         #ffffff;

$color-border:          #e2e8f0;
$color-border-card:     #dde3ee;
$color-border-alt:      #e8edf3;
$color-border-strong:   #c8d5e8;

// ── Purple (batch accent) ────────────────────────────────────────────────
$color-purple:          #7c3aed;
$color-purple-dark:     #6d28d9;
$color-purple-light:    #f5f3ff;

// ── Hero gradient ────────────────────────────────────────────────────────
$hero-gradient:         linear-gradient(160deg, #040c22 0%, #091535 35%, #0b1e50 65%, #091535 100%);

// ── Typography ───────────────────────────────────────────────────────────
$font-family:           'Tajawal', sans-serif;
$font-size-xs:          10px;
$font-size-sm:          11px;
$font-size-base:        12.5px;
$font-size-md:          13px;
$font-size-lg:          14px;
$font-size-xl:          15px;
$font-size-2xl:         16px;
$font-size-3xl:         22px;

// ── Spacing ──────────────────────────────────────────────────────────────
$space-1:   4px;
$space-2:   8px;
$space-3:   12px;
$space-4:   16px;
$space-5:   20px;
$space-6:   24px;
$space-7:   28px;
$space-8:   32px;

// ── Radius ───────────────────────────────────────────────────────────────
$radius-sm:   6px;
$radius-md:   8px;
$radius-lg:   10px;
$radius-xl:   12px;
$radius-2xl:  14px;

// ── Shadow ───────────────────────────────────────────────────────────────
$shadow-card:   0 1px 3px rgba(0,0,0,.08), 0 1px 2px rgba(0,0,0,.05);
$shadow-hover:  0 4px 16px rgba(0,0,0,.07);
$shadow-lg:     0 12px 40px rgba(0,0,0,.14);
$shadow-blue:   0 3px 10px rgba(26,58,143,.25);

// ── Layout ───────────────────────────────────────────────────────────────
$max-width:     1280px;
$header-height: 64px;
```

---

### Task 2: Create `_reset.scss`

**File:** `app/styles/_reset.scss` (create)

- [ ] Write `_reset.scss`:

```scss
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html, body {
  font-family: $font-family;
  color: $color-text-body;
  background: $color-bg-page;
  direction: rtl;
}
```

---

### Task 3: Create `_layout.scss`

**File:** `app/styles/_layout.scss` (create)

- [ ] Write `_layout.scss` with all layout primitives extracted from `app/index.html`. Copy every rule under the `TOP HEADER BAR`, `BREADCRUMB BAR`, `PAGE HERO BANNER`, `PAGE BODY`, and `FOOTER` comment blocks verbatim, converted to class selectors:

Key classes to include:
- `.top-header`, `.top-header-inner`
- `.logo-wrap`, `.logo-icon`, `.logo-text`
- `.top-nav`, `.nav-link`, `.nav-link:hover`, `.nav-link.active`, `.nav-link.active::after`, `.nav-link i`, `.nav-badge`
- `.top-right`, `.notif-btn`, `.notif-btn:hover`, `.notif-dot`
- `.lang-toggle`, `.lang-toggle button`, `.lang-toggle button.active`
- `.user-chip`, `.user-chip:hover`, `.user-avatar`, `.user-info`, `.user-info .uname`, `.user-info .urole`, `.user-chip .chevron`
- `.breadcrumb-bar`, `.breadcrumb-inner`, `.breadcrumb-inner a`, `.breadcrumb-inner .sep`, `.breadcrumb-inner .cur`
- `.page-hero`, `.page-hero::before`, `.page-hero::after`
- `.hero-inner`, `.hero-text h2`, `.hero-text p`
- `.hero-agency`, `.hero-agency-icon`, `.hero-agency-text .aname`, `.hero-agency-text .amid`, `.hero-badge`
- `.page-body`, `.section-title`, `.section-title::after`, `.section-title i`
- `.footer`, `.footer a`, `.footer-links`

---

### Task 4: Create `_components.scss`

**File:** `app/styles/_components.scss` (create)

- [ ] Write `_components.scss` with all reusable component rules from `app/index.html`:

Key classes to include:
- Cards: `.card`, `.card:hover`, `.card-header`, `.card-icon`, `.card-label`, `.card-value`, `.card-sub`, `.card-action`
- Card colour modifiers: `.ci-icon`, `.cw-icon`, `.cw-value`, `.cw-action`, `.cw-action:hover`, `.ct-icon`, `.cr-icon`
- `.cards-row`, `.stats-row`, `.actions-row`
- Stats: `.stat`, `.stat-icon-wrap`, `.stat-body`, `.stat-label`, `.stat-num`, `.stat-period`
- Stat colour modifiers: `.stat-blue`, `.stat-amber`, `.stat-green`, `.stat-red` (with all nested selectors)
- Badges/Pills: `.pill`, `.pill i`, `.pill-green`, `.pill-amber`, `.pill-blue`, `.pill-red`
- `.type-badge`, `.type-single`, `.type-batch`
- Action cards: `.action-card`, `.action-card:hover`, `.action-icon`, `.action-card h4`, `.action-card p`, `.action-btn`
- Action card modifiers: `.ac-new`, `.ac-batch`, `.ac-wallet` (with icon/btn/hover sub-rules)
- Tables: `.table-card`, `.table-header`, `.table-header h3`, `.table-header a`, `table`, `thead tr`, `th`, `td`, `tbody tr:last-child td`, `tbody tr`, `tbody tr:hover`, `.ref-cell`, `.tbl-link`, `.retry-link`
- Alerts: `.pending-alert`, `.pending-alert-icon`, `.pending-alert-text`, `.pending-alert-actions`, `.pending-cta`, `.pending-cta:hover`, `.dismiss-btn`, `.dismiss-btn:hover`
- Status: `.status-row`, `.status-dot`
- Dropdowns: `.notif-drawer`, `.notif-drawer.open`, `@keyframes slideIn`, `.nd-header`, `.nd-mark`, `.nd-item`, `.nd-item:hover`, `.nd-dot`, `.nd-text strong`, `.nd-text span`, `.nd-time`
- `.user-dropdown`, `.user-dropdown.open`, `.ud-top`, `.ud-top .udname`, `.ud-top .udrole`, `.ud-top .udid`, `.ud-item`, `.ud-item:hover`, `.ud-item i`, `.ud-item.logout`

Add these **new components** needed by batch-inquiry.html:

```scss
/* ── Buttons ── */
.btn {
  display: inline-flex; align-items: center; justify-content: center; gap: 7px;
  height: 42px; padding: 0 20px; border: none; border-radius: $radius-lg;
  font-family: $font-family; font-size: $font-size-md; font-weight: 700;
  cursor: pointer; transition: background .2s, box-shadow .2s; white-space: nowrap;
}
.btn-primary {
  background: $color-primary-mid; color: #fff; box-shadow: $shadow-blue;
  &:hover { background: darken(#1a3a8f, 5%); }
}
.btn-secondary {
  background: $color-bg-alt; color: $color-text-secondary;
  border: 1.5px solid $color-border;
  &:hover { background: $color-bg-page; }
}
.btn-success {
  background: $color-success; color: #fff;
  box-shadow: 0 3px 10px rgba(22,163,74,.2);
  &:hover { background: $color-success-dark; }
}
.btn-danger {
  background: $color-danger; color: #fff;
  &:hover { background: $color-danger-dark; }
}
.btn-sm { height: 36px; padding: 0 14px; font-size: $font-size-base; }
.btn-lg { height: 50px; padding: 0 28px; font-size: $font-size-xl; }
.btn-full { width: 100%; }
.btn:disabled {
  opacity: .45; cursor: not-allowed; box-shadow: none;
}

/* ── Wallet chip ── */
.wallet-chip {
  display: inline-flex; align-items: center; gap: 8px;
  background: $color-success-bg; border: 1.5px solid $color-success-border;
  border-radius: $radius-lg; padding: 8px 14px; font-size: $font-size-md;
}
.wallet-chip i { color: $color-success; font-size: 15px; }
.wallet-chip .wc-label { color: $color-success-dark; font-weight: 600; }
.wallet-chip .wc-amount { font-size: $font-size-xl; font-weight: 800; color: $color-success; }

/* ── Alert boxes ── */
.alert {
  display: flex; align-items: flex-start; gap: 12px;
  padding: 14px 16px; border-radius: $radius-lg; border: 1.5px solid;
  margin-bottom: $space-5;
}
.alert-icon { font-size: 18px; flex-shrink: 0; margin-top: 1px; }
.alert-body { flex: 1; }
.alert-body strong { display: block; font-size: $font-size-md; font-weight: 700; margin-bottom: 2px; }
.alert-body span { font-size: $font-size-base; line-height: 1.6; }
.alert-success {
  background: $color-success-bg; border-color: $color-success-border;
  .alert-icon, .alert-body strong { color: $color-success-dark; }
  .alert-body span { color: $color-success; }
}
.alert-warning {
  background: $color-warning-bg; border-color: $color-warning-border;
  .alert-icon, .alert-body strong { color: $color-warning-deep; }
  .alert-body span { color: $color-warning-dark; }
}
.alert-danger {
  background: $color-danger-bg; border-color: $color-danger-border;
  .alert-icon, .alert-body strong { color: $color-danger-dark; }
  .alert-body span { color: $color-danger; }
}
.alert-info {
  background: $color-info-light; border-color: $color-primary-muted;
  .alert-icon, .alert-body strong { color: $color-primary-mid; }
  .alert-body span { color: $color-primary; }
}

/* ── Summary chip row ── */
.summary-chips { display: flex; gap: $space-3; flex-wrap: wrap; margin-top: $space-3; }
.summary-chip {
  display: inline-flex; align-items: center; gap: 5px;
  padding: 4px 12px; border-radius: 20px; font-size: $font-size-base; font-weight: 700;
}
.chip-total   { background: $color-primary-light; color: $color-primary; }
.chip-ok      { background: $color-success-light; color: $color-success-dark; }
.chip-error   { background: $color-danger-light; color: $color-danger-dark; }
```

---

### Task 5: Create `_wizard.scss`

**File:** `app/styles/_wizard.scss` (create)

- [ ] Write `_wizard.scss`:

```scss
/* ══ Wizard Container ══════════════════════════════════════════════════ */
.wizard-wrap {
  background: $color-bg-card;
  border: 1.5px solid $color-border-card;
  border-radius: $radius-xl;
  overflow: hidden;
  box-shadow: $shadow-card;
}

/* ══ Stepper Bar ══════════════════════════════════════════════════════ */
.stepper {
  display: flex; align-items: center;
  padding: $space-6 $space-7;
  background: $color-bg-alt;
  border-bottom: 1px solid $color-border;
  gap: 0;
}

.step-node {
  display: flex; flex-direction: column; align-items: center; gap: 7px;
  flex-shrink: 0; cursor: default;
}
.step-circle {
  width: 40px; height: 40px; border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 15px; font-weight: 800; border: 2px solid;
  transition: all .25s;
}
.step-label {
  font-size: $font-size-sm; font-weight: 600; white-space: nowrap;
  transition: color .25s;
}

/* Locked state */
.step-node.locked .step-circle {
  background: $color-bg-alt; border-color: $color-border;
  color: $color-text-subtle;
}
.step-node.locked .step-label { color: $color-text-subtle; }

/* Active state */
.step-node.active .step-circle {
  background: $color-primary-mid; border-color: $color-primary-mid;
  color: #fff; box-shadow: $shadow-blue;
}
.step-node.active .step-label { color: $color-primary-mid; font-weight: 700; }

/* Completed state */
.step-node.completed .step-circle {
  background: $color-success-light; border-color: $color-success;
  color: $color-success;
}
.step-node.completed .step-label { color: $color-success-dark; }
.step-node.completed { cursor: pointer; }
.step-node.completed:hover .step-circle {
  background: $color-success; color: #fff;
}

/* Connector line */
.step-line {
  flex: 1; height: 2px; background: $color-border; margin-bottom: 28px;
  transition: background .25s;
}
.step-line.completed { background: $color-success; }
.step-line.active    { background: $color-primary-muted; }

/* ══ Step Panels ══════════════════════════════════════════════════════ */
.step-panels { padding: $space-7; }
.step-panel { display: none; }
.step-panel.active { display: block; animation: fadeIn .2s ease; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: translateY(0); } }

/* ── Step panel heading ── */
.step-heading {
  margin-bottom: $space-5;
}
.step-heading h3 {
  font-size: $font-size-2xl; font-weight: 800; color: $color-text-primary;
  margin-bottom: 4px;
}
.step-heading p { font-size: $font-size-md; color: $color-text-muted; }

/* ── Step footer (nav buttons) ── */
.step-footer {
  display: flex; align-items: center; justify-content: space-between;
  padding: $space-5 $space-7;
  border-top: 1px solid $color-border;
  background: $color-bg-alt;
}
.step-footer-right { display: flex; gap: $space-3; }

/* ══ Step 1: Download Template ════════════════════════════════════════ */
.template-info-card {
  background: $color-primary-light; border: 1.5px solid $color-primary-muted;
  border-radius: $radius-lg; padding: $space-5 $space-6; margin-bottom: $space-5;
}
.template-info-card h4 { font-size: $font-size-xl; font-weight: 800; color: $color-primary-dark; margin-bottom: 8px; }
.template-info-card p  { font-size: $font-size-md; color: $color-primary; line-height: 1.7; }

.download-btns { display: flex; gap: $space-3; margin-bottom: $space-5; }

.columns-toggle {
  display: flex; align-items: center; gap: 7px;
  font-size: $font-size-md; color: $color-primary; font-weight: 600;
  cursor: pointer; background: none; border: none; font-family: $font-family;
  margin-bottom: $space-3; padding: 0;
}
.columns-toggle:hover { text-decoration: underline; }

.columns-table-wrap { overflow: hidden; }
.columns-table-wrap.hidden { display: none; }
.columns-table {
  width: 100%; border-collapse: collapse;
  border: 1px solid $color-border; border-radius: $radius-lg; overflow: hidden;
}
.columns-table th {
  background: $color-bg-alt; padding: 9px 14px;
  font-size: $font-size-sm; font-weight: 700; color: $color-text-muted;
  text-align: right; border-bottom: 1px solid $color-border;
  text-transform: uppercase; letter-spacing: .4px;
}
.columns-table td {
  padding: 9px 14px; font-size: $font-size-md; color: $color-text-body;
  border-bottom: 1px solid $color-bg-alt;
}
.columns-table tbody tr:last-child td { border-bottom: none; }
.col-req  { display: inline-block; padding: 2px 8px; border-radius: 4px; font-size: 10px; font-weight: 700; }
.col-req.required    { background: $color-danger-light; color: $color-danger-dark; }
.col-req.optional    { background: $color-bg-alt; color: $color-text-muted; border: 1px solid $color-border; }
.col-req.conditional { background: $color-warning-light; color: $color-warning-dark; }

/* ══ Step 2: Upload & Validate ═════════════════════════════════════════ */
.dropzone {
  border: 2px dashed $color-border-strong;
  border-radius: $radius-xl; padding: $space-8;
  text-align: center; cursor: pointer;
  transition: border-color .2s, background .2s;
  background: $color-bg-alt; margin-bottom: $space-5;
}
.dropzone:hover, .dropzone.drag-over {
  border-color: $color-primary; background: $color-primary-light;
}
.dropzone-icon { font-size: 40px; color: $color-text-subtle; margin-bottom: $space-3; }
.dropzone h4 { font-size: $font-size-xl; font-weight: 800; color: $color-text-primary; margin-bottom: 6px; }
.dropzone p  { font-size: $font-size-md; color: $color-text-muted; margin-bottom: $space-4; }
.dropzone small { font-size: $font-size-sm; color: $color-text-placeholder; }
.dropzone input[type="file"] { display: none; }

/* Upload loading overlay on dropzone */
.dropzone.loading {
  pointer-events: none; position: relative;
}
.dropzone.loading::after {
  content: ''; position: absolute; inset: 0;
  background: rgba(255,255,255,.75); border-radius: inherit;
}

/* Error state */
.error-table-wrap { margin-top: $space-4; }
.error-count-badge {
  display: inline-flex; align-items: center; gap: 5px;
  background: $color-danger-light; color: $color-danger-dark;
  border-radius: 20px; padding: 4px 12px;
  font-size: $font-size-base; font-weight: 700; margin-bottom: $space-3;
}
.reupload-zone {
  border: 2px dashed $color-danger-border;
  border-radius: $radius-xl; padding: $space-6;
  text-align: center; cursor: pointer; background: $color-danger-bg;
  transition: border-color .2s;
  margin-top: $space-5;
}
.reupload-zone:hover { border-color: $color-danger; }
.reupload-zone input[type="file"] { display: none; }

/* ══ Step 3: Fee Review ════════════════════════════════════════════════ */
.fee-table-card {
  background: $color-bg-card; border: 1.5px solid $color-border-card;
  border-radius: $radius-lg; overflow: hidden; margin-bottom: $space-5;
}
.fee-table { width: 100%; border-collapse: collapse; }
.fee-table th {
  background: $color-bg-alt; padding: 11px 16px; text-align: right;
  font-size: $font-size-sm; font-weight: 700; color: $color-text-muted;
  border-bottom: 1px solid $color-border; text-transform: uppercase; letter-spacing: .4px;
}
.fee-table td { padding: 13px 16px; font-size: $font-size-md; color: $color-text-body; border-bottom: 1px solid $color-bg-alt; }
.fee-table tbody tr:last-child td { border-bottom: none; }
.fee-table .fee-total-row td {
  border-top: 2px solid $color-border; font-size: $font-size-lg; font-weight: 800;
  color: $color-text-primary; background: $color-bg-alt;
}
.non-refundable-note {
  display: inline-flex; align-items: center; gap: 6px;
  background: $color-warning-bg; border: 1px solid $color-warning-border;
  border-radius: $radius-lg; padding: 8px 14px;
  font-size: $font-size-base; color: $color-warning-dark; font-weight: 600;
  margin-bottom: $space-5;
}

/* ══ Step 4: Confirm & Deduct ══════════════════════════════════════════ */
.confirm-summary-card {
  background: $color-bg-alt; border: 1.5px solid $color-border;
  border-radius: $radius-lg; padding: $space-5 $space-6; margin-bottom: $space-5;
}
.confirm-summary-card .cs-row {
  display: flex; align-items: center; justify-content: space-between;
  padding: 9px 0; border-bottom: 1px solid $color-border;
}
.confirm-summary-card .cs-row:last-child { border-bottom: none; }
.confirm-summary-card .cs-label { font-size: $font-size-md; color: $color-text-muted; }
.confirm-summary-card .cs-value { font-size: $font-size-md; font-weight: 700; color: $color-text-primary; }
.confirm-summary-card .cs-value.total { font-size: $font-size-xl; color: $color-primary; }
.confirm-summary-card .cs-value.balance-after { color: $color-success; }

.irreversible-warning {
  display: flex; align-items: center; gap: 8px;
  background: $color-warning-bg; border: 1.5px solid $color-warning-border;
  border-radius: $radius-lg; padding: 12px 16px; margin-bottom: $space-5;
  font-size: $font-size-md; color: $color-warning-deep; font-weight: 600;
}

/* Success screen */
.success-screen {
  text-align: center; padding: $space-8 $space-6;
}
.success-icon-wrap {
  width: 80px; height: 80px; border-radius: 50%;
  background: $color-success-light; border: 3px solid $color-success;
  display: flex; align-items: center; justify-content: center;
  margin: 0 auto $space-5; font-size: 36px; color: $color-success;
}
.success-screen h3 { font-size: $font-size-3xl; font-weight: 800; color: $color-text-primary; margin-bottom: 8px; }
.success-screen p  { font-size: $font-size-lg; color: $color-text-muted; margin-bottom: $space-6; }
.success-detail-card {
  display: inline-block; text-align: right; min-width: 340px;
  background: $color-bg-alt; border: 1.5px solid $color-border;
  border-radius: $radius-lg; padding: $space-5; margin-bottom: $space-6;
}
.success-detail-card .sd-row {
  display: flex; align-items: center; justify-content: space-between;
  gap: $space-5; padding: 7px 0; border-bottom: 1px solid $color-border;
  font-size: $font-size-md;
}
.success-detail-card .sd-row:last-child { border-bottom: none; }
.success-detail-card .sd-label { color: $color-text-muted; }
.success-detail-card .sd-value { font-weight: 700; color: $color-text-primary; }
.success-actions { display: flex; gap: $space-3; justify-content: center; flex-wrap: wrap; }
.success-new-link {
  display: block; margin-top: $space-4;
  font-size: $font-size-md; color: $color-primary; font-weight: 600;
  text-decoration: none;
}
.success-new-link:hover { text-decoration: underline; }

/* System error screen */
.error-screen { text-align: center; padding: $space-8 $space-6; }
.error-icon-wrap {
  width: 80px; height: 80px; border-radius: 50%;
  background: $color-danger-light; border: 3px solid $color-danger-border;
  display: flex; align-items: center; justify-content: center;
  margin: 0 auto $space-5; font-size: 36px; color: $color-danger;
}
.error-ref {
  display: inline-block; font-size: $font-size-base; font-weight: 700;
  background: $color-danger-light; color: $color-danger-dark;
  padding: 4px 14px; border-radius: 20px; margin-bottom: $space-5;
  letter-spacing: .5px;
}
```

---

### Task 6: Create `main.scss`

**File:** `app/styles/main.scss` (create)

- [ ] Write `main.scss`:

```scss
// Security Clearance & Inquiry System — Portal Design System
// Compiled to main.css for browser use

@use 'tokens';
@use 'reset';
@use 'layout';
@use 'components';
@use 'wizard';
```

---

### Task 7: Write `main.css` (pre-compiled)

**File:** `app/styles/main.css` (create)

- [ ] Write `main.css` as the flat compiled output of all five partials. It is the **only file the HTML pages reference**. Content is identical to what a SCSS compiler would produce from `main.scss` — paste all CSS rules in order: tokens → reset → layout → components → wizard. Replace all SCSS variables with their literal values. Remove SCSS syntax (`$`, `&`, `@use`).

The resulting file should contain every CSS rule from Tasks 1–5 with variables resolved to their hex/px values.

- [ ] Verify by opening `app/index.html` in browser — must look identical to its current appearance.

---

### Task 8: Refactor `app/index.html`

**File:** `app/index.html` (modify)

- [ ] Remove the entire `<style>` block (lines 11–468 in current file)
- [ ] Add two lines after the Font Awesome CDN link:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;800&display=swap" rel="stylesheet">
<link rel="stylesheet" href="styles/main.css">
```
- [ ] Open in browser — must look pixel-identical to before the refactor
- [ ] If any class is missing from `main.css`, add it and update `_components.scss` or `_layout.scss` accordingly

---

### Task 9: Refactor `app/registration.html`

**File:** `app/registration.html` (modify)

- [ ] Read the file, identify the `<style>` block
- [ ] Extract any classes that are not already in `main.css` and add them to the appropriate SCSS partial + recompile `main.css`
- [ ] Remove the `<style>` block, add `<link rel="stylesheet" href="styles/main.css">`
- [ ] Open in browser — must look identical to before

---

### Task 10: Commit design system

```bash
git add app/styles/ app/index.html app/registration.html
git commit -m "feat: extract shared CSS design system into SCSS partials + main.css

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Chunk 2: Page Skeleton, Header, Stepper + Step 1

### Task 11: Create `app/batch-inquiry.html` skeleton

**File:** `app/batch-inquiry.html` (create)

- [ ] Create the file with this skeleton (no step panel content yet):

```html
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>استعلام دفعي جديد — نظام الموافقات الأمنية</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>

<!-- HEADER — copy exact HTML from index.html,
     change active nav link to "رفع دفعي" -->

<!-- NOTIFICATION DRAWER — copy exact HTML from index.html -->
<!-- USER DROPDOWN — copy exact HTML from index.html -->

<!-- BREADCRUMB -->
<div class="breadcrumb-bar">
    <div class="breadcrumb-inner">
        <a href="index.html"><i class="fas fa-home" style="font-size:11px;"></i></a>
        <i class="fas fa-chevron-left sep"></i>
        <a href="#">الاستعلامات الدفعية</a>
        <i class="fas fa-chevron-left sep"></i>
        <span class="cur">استعلام دفعي جديد</span>
    </div>
</div>

<!-- PAGE HERO -->
<div class="page-hero">
    <div class="hero-inner">
        <div class="hero-text">
            <h2>استعلام دفعي جديد</h2>
            <p>اتبع الخطوات لرفع ملف المسافرين واستكمال الإرسال</p>
        </div>
        <div class="hero-agency">
            <div class="hero-agency-icon"><i class="fas fa-building"></i></div>
            <div class="hero-agency-text">
                <div class="aname">وكالة النيل للسياحة</div>
                <div class="amid">AG-2024-0156</div>
                <div class="hero-badge"><i class="fas fa-circle-check"></i> وكالة معتمدة</div>
            </div>
        </div>
    </div>
</div>

<!-- PAGE BODY -->
<div class="page-body">

    <!-- Wallet + page title row -->
    <div style="display:flex; align-items:center; justify-content:space-between; margin-bottom:20px;">
        <h2 class="section-title" style="margin-bottom:0;">
            <i class="fas fa-layer-group"></i> استعلام دفعي جديد
        </h2>
        <div class="wallet-chip">
            <i class="fas fa-wallet"></i>
            <span class="wc-label">رصيد المحفظة:</span>
            <span class="wc-amount">12,500 جنيه</span>
        </div>
    </div>

    <!-- Wizard -->
    <div class="wizard-wrap">

        <!-- STEPPER -->
        <div class="stepper">
            <div class="step-node active" id="node-1" onclick="goToStep(1)">
                <div class="step-circle">1</div>
                <div class="step-label">تحميل النموذج</div>
            </div>
            <div class="step-line" id="line-1-2"></div>
            <div class="step-node locked" id="node-2" onclick="goToStep(2)">
                <div class="step-circle"><i class="fas fa-lock"></i></div>
                <div class="step-label">رفع الملف والتدقيق</div>
            </div>
            <div class="step-line" id="line-2-3"></div>
            <div class="step-node locked" id="node-3" onclick="goToStep(3)">
                <div class="step-circle"><i class="fas fa-lock"></i></div>
                <div class="step-label">مراجعة الرسوم</div>
            </div>
            <div class="step-line" id="line-3-4"></div>
            <div class="step-node locked" id="node-4" onclick="goToStep(4)">
                <div class="step-circle"><i class="fas fa-lock"></i></div>
                <div class="step-label">تأكيد الخصم</div>
            </div>
        </div>

        <!-- STEP PANELS (populated in later tasks) -->
        <div class="step-panels">
            <div class="step-panel active" id="panel-1"><!-- Task 12 --></div>
            <div class="step-panel" id="panel-2"><!-- Task 13 --></div>
            <div class="step-panel" id="panel-3"><!-- Task 15 --></div>
            <div class="step-panel" id="panel-4"><!-- Task 16 --></div>
        </div>

        <!-- STEP FOOTER -->
        <div class="step-footer">
            <button class="btn btn-secondary btn-sm" id="btn-back" onclick="goBack()" style="visibility:hidden;">
                <i class="fas fa-arrow-right"></i> السابق
            </button>
            <div class="step-footer-right">
                <button class="btn btn-secondary btn-sm" onclick="cancelWizard()">إلغاء</button>
                <button class="btn btn-primary btn-sm" id="btn-next" onclick="goNext()">
                    التالي <i class="fas fa-arrow-left"></i>
                </button>
            </div>
        </div>
    </div>

</div>

<!-- FOOTER — copy exact HTML from index.html -->

<script>
// Wizard state — populated in Task 17
</script>

</body>
</html>
```

- [ ] Open in browser — header, breadcrumb, hero, wallet chip, stepper, and footer must all render correctly
- [ ] Verify stepper shows step 1 as active (blue), steps 2–4 locked (gray with lock icon), connector lines gray

---

### Task 12: Build Step 1 panel — Download Template

**File:** `app/batch-inquiry.html` — `#panel-1` (modify)

- [ ] Replace the `<!-- Task 12 -->` comment with:

```html
<div class="step-heading">
    <h3><i class="fas fa-file-arrow-down" style="color:#1d4ed8;margin-left:8px;"></i>تحميل نموذج الملف الدفعي</h3>
    <p>قم بتحميل النموذج الرسمي المكوّن من 21 عموداً، واملأ بيانات المسافرين، ثم انتقل للخطوة التالية لرفعه.</p>
</div>

<div class="template-info-card">
    <h4><i class="fas fa-circle-info" style="margin-left:6px;"></i>هيكل النموذج</h4>
    <p>
        يحتوي النموذج على <strong>21 عموداً</strong>: 9 أعمدة أساسية لبيانات المسافر والرحلة،
        و12 عموداً لمحطات العبور (3 محطات × 4 حقول لكل محطة).
        يمكن رفع ملف بصيغة <strong>.xlsx</strong> أو <strong>.csv</strong>.
    </p>
</div>

<div class="download-btns">
    <a href="assets/batch-template.xlsx" download class="btn btn-primary">
        <i class="fas fa-file-excel"></i> تحميل النموذج (.xlsx)
    </a>
    <a href="assets/batch-template.csv" download class="btn btn-secondary">
        <i class="fas fa-file-csv"></i> تحميل النموذج (.csv)
    </a>
</div>

<button class="columns-toggle" onclick="toggleColumns()" id="col-toggle-btn">
    <i class="fas fa-chevron-down" id="col-toggle-icon"></i>
    عرض أعمدة النموذج وتفاصيلها
</button>

<div class="columns-table-wrap hidden" id="columns-table-wrap">
    <table class="columns-table">
        <thead>
            <tr>
                <th>#</th>
                <th>اسم العمود</th>
                <th>الحالة</th>
                <th>الوصف</th>
            </tr>
        </thead>
        <tbody>
            <tr><td>1</td><td dir="ltr">Traveler_Name</td><td><span class="col-req required">مطلوب</span></td><td>الاسم الكامل كما في الجواز</td></tr>
            <tr><td>2</td><td dir="ltr">Nationality</td><td><span class="col-req required">مطلوب</span></td><td>الجنسية (رمز ISO أو الاسم الكامل)</td></tr>
            <tr><td>3</td><td dir="ltr">Passport_Number</td><td><span class="col-req required">مطلوب</span></td><td>رقم جواز السفر (6–20 حرفاً)</td></tr>
            <tr><td>4</td><td dir="ltr">Passport_Expiry</td><td><span class="col-req required">مطلوب</span></td><td>تاريخ انتهاء الجواز (YYYY-MM-DD)</td></tr>
            <tr><td>5</td><td dir="ltr">Travel_Date</td><td><span class="col-req required">مطلوب</span></td><td>تاريخ السفر (YYYY-MM-DD)</td></tr>
            <tr><td>6</td><td dir="ltr">Departure_Country</td><td><span class="col-req required">مطلوب</span></td><td>دولة المغادرة</td></tr>
            <tr><td>7</td><td dir="ltr">Arrival_Airport_Egypt</td><td><span class="col-req required">مطلوب</span></td><td>مطار الوصول في مصر</td></tr>
            <tr><td>8</td><td dir="ltr">Purpose</td><td><span class="col-req required">مطلوب</span></td><td>Tourism / Business / Medical / Family / Education / Conference / Transit / Other</td></tr>
            <tr><td>9</td><td dir="ltr">Flight_Number</td><td><span class="col-req optional">اختياري</span></td><td>رقم الرحلة (حتى 10 أحرف)</td></tr>
            <tr><td>10</td><td dir="ltr">Transit_Stop_1_Country</td><td><span class="col-req conditional">مشروط</span></td><td>مطلوب إذا وُجدت محطة عبور 1</td></tr>
            <tr><td>11</td><td dir="ltr">Transit_Stop_1_Airport</td><td><span class="col-req optional">اختياري</span></td><td>مطار العبور 1</td></tr>
            <tr><td>12</td><td dir="ltr">Transit_Stop_1_Hours</td><td><span class="col-req optional">اختياري</span></td><td>مدة التوقف بالساعات (0–72)</td></tr>
            <tr><td>13</td><td dir="ltr">Transit_Stop_1_Flight</td><td><span class="col-req optional">اختياري</span></td><td>رقم رحلة الاتصال 1</td></tr>
            <tr><td>14</td><td dir="ltr">Transit_Stop_2_Country</td><td><span class="col-req conditional">مشروط</span></td><td>مطلوب إذا وُجدت محطة عبور 2</td></tr>
            <tr><td>15</td><td dir="ltr">Transit_Stop_2_Airport</td><td><span class="col-req optional">اختياري</span></td><td>مطار العبور 2</td></tr>
            <tr><td>16</td><td dir="ltr">Transit_Stop_2_Hours</td><td><span class="col-req optional">اختياري</span></td><td>مدة التوقف بالساعات (0–72)</td></tr>
            <tr><td>17</td><td dir="ltr">Transit_Stop_2_Flight</td><td><span class="col-req optional">اختياري</span></td><td>رقم رحلة الاتصال 2</td></tr>
            <tr><td>18</td><td dir="ltr">Transit_Stop_3_Country</td><td><span class="col-req conditional">مشروط</span></td><td>مطلوب إذا وُجدت محطة عبور 3</td></tr>
            <tr><td>19</td><td dir="ltr">Transit_Stop_3_Airport</td><td><span class="col-req optional">اختياري</span></td><td>مطار العبور 3</td></tr>
            <tr><td>20</td><td dir="ltr">Transit_Stop_3_Hours</td><td><span class="col-req optional">اختياري</span></td><td>مدة التوقف بالساعات (0–72)</td></tr>
            <tr><td>21</td><td dir="ltr">Transit_Stop_3_Flight</td><td><span class="col-req optional">اختياري</span></td><td>رقم رحلة الاتصال 3</td></tr>
        </tbody>
    </table>
</div>
```

- [ ] Verify in browser: info card renders, two download buttons visible, toggle button expands/collapses the column table

---

### Task 13: Commit skeleton + Step 1

```bash
git add app/batch-inquiry.html
git commit -m "feat: add batch inquiry wizard skeleton with Step 1 (download template)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Chunk 3: Step 2 — Upload & Validate

### Task 14: Build Step 2 panel — all states

**File:** `app/batch-inquiry.html` — `#panel-2` (modify)

- [ ] Replace the `<!-- Task 13 -->` comment with the full Step 2 panel. The panel has four sub-states controlled by JS: `upload` (default), `loading`, `success`, `error`.

```html
<div class="step-heading">
    <h3><i class="fas fa-file-arrow-up" style="color:#1d4ed8;margin-left:8px;"></i>رفع الملف والتدقيق</h3>
    <p>ارفع ملف المسافرين بصيغة .xlsx أو .csv. سيتم التحقق من كل صف تلقائياً.</p>
</div>

<!-- STATE: upload (default) -->
<div id="s2-upload">
    <label class="dropzone" id="dropzone" for="file-input"
           ondragover="handleDragOver(event)" ondragleave="handleDragLeave(event)" ondrop="handleDrop(event)">
        <div class="dropzone-icon"><i class="fas fa-cloud-arrow-up"></i></div>
        <h4>اسحب الملف وأفلته هنا</h4>
        <p>أو انقر لاختيار الملف من جهازك</p>
        <span>يُقبل: .xlsx، .csv — الحجم الأقصى 10 ميجابايت</span>
        <input type="file" id="file-input" accept=".xlsx,.csv" onchange="handleFileSelect(event)">
    </label>
    <div style="margin-top:8px;" id="selected-file-name"></div>
    <button class="btn btn-primary" id="btn-upload" style="margin-top:16px;" onclick="simulateValidation()" disabled>
        <i class="fas fa-magnifying-glass"></i> رفع الملف والتحقق
    </button>
    <!-- Demo controls for review purposes -->
    <div style="margin-top:20px; padding:12px 16px; background:#f8fafc; border:1px dashed #cbd5e1; border-radius:8px; font-size:12px; color:#64748b;">
        <strong>أدوات العرض التجريبي:</strong>
        <button onclick="showS2Success()" style="margin-right:8px; padding:4px 10px; font-size:11px; cursor:pointer; border:1px solid #cbd5e1; border-radius:4px; background:#fff;">✓ نجاح التحقق</button>
        <button onclick="showS2Error()" style="padding:4px 10px; font-size:11px; cursor:pointer; border:1px solid #cbd5e1; border-radius:4px; background:#fff;">✗ أخطاء في التحقق</button>
    </div>
</div>

<!-- STATE: loading -->
<div id="s2-loading" style="display:none; text-align:center; padding:48px;">
    <div style="font-size:48px; color:#1d4ed8; margin-bottom:16px;">
        <i class="fas fa-spinner fa-spin"></i>
    </div>
    <h4 style="font-size:16px; font-weight:700; color:#0f172a; margin-bottom:6px;">جارٍ التحقق من الملف…</h4>
    <p style="font-size:13px; color:#64748b;">يُرجى الانتظار بينما يتم فحص كل صف من بيانات المسافرين</p>
</div>

<!-- STATE: success -->
<div id="s2-success" style="display:none;">
    <div class="alert alert-success">
        <div class="alert-icon"><i class="fas fa-circle-check"></i></div>
        <div class="alert-body">
            <strong>تم التحقق بنجاح — جميع الصفوف صالحة</strong>
            <span>يمكنك المتابعة إلى مرحلة مراجعة الرسوم.</span>
        </div>
    </div>
    <div class="summary-chips">
        <span class="summary-chip chip-total"><i class="fas fa-users"></i> إجمالي الصفوف: 47</span>
        <span class="summary-chip chip-ok"><i class="fas fa-circle-check"></i> مقبول: 47</span>
    </div>
    <div style="margin-top:16px; padding:12px 16px; background:#f8fafc; border:1.5px solid #e2e8f0; border-radius:8px; display:flex; align-items:center; gap:12px;">
        <i class="fas fa-file-excel" style="color:#16a34a; font-size:24px;"></i>
        <div>
            <div style="font-size:13px; font-weight:700; color:#0f172a;" id="success-filename">batch_travelers.xlsx</div>
            <div style="font-size:11px; color:#64748b;">47 مسافراً · تم الرفع بنجاح</div>
        </div>
        <button onclick="resetUpload()" style="margin-right:auto; font-size:12px; color:#64748b; background:none; border:none; cursor:pointer; text-decoration:underline;">رفع ملف مختلف</button>
    </div>
</div>

<!-- STATE: error -->
<div id="s2-error" style="display:none;">
    <div class="alert alert-danger">
        <div class="alert-icon"><i class="fas fa-circle-xmark"></i></div>
        <div class="alert-body">
            <strong>فشل التحقق — يجب تصحيح جميع الأخطاء قبل المتابعة</strong>
            <span>تحقق من الأخطاء أدناه، صحّح الملف، ثم أعد رفعه. لا يمكن المتابعة مع وجود أخطاء.</span>
        </div>
    </div>
    <div class="summary-chips">
        <span class="summary-chip chip-total"><i class="fas fa-users"></i> إجمالي الصفوف: 50</span>
        <span class="summary-chip chip-ok"><i class="fas fa-circle-check"></i> مقبول: 47</span>
        <span class="summary-chip chip-error"><i class="fas fa-circle-xmark"></i> أخطاء: 3</span>
    </div>

    <div class="error-table-wrap">
        <div style="display:flex; align-items:center; justify-content:space-between; margin-top:16px; margin-bottom:8px;">
            <div class="error-count-badge"><i class="fas fa-triangle-exclamation"></i> 3 أخطاء تحتاج تصحيحاً</div>
        </div>
        <div class="table-card">
            <table>
                <thead>
                    <tr>
                        <th>رقم الصف</th>
                        <th>اسم العمود</th>
                        <th>الخطأ</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><span class="pill pill-red">الصف 5</span></td>
                        <td dir="ltr" style="font-weight:600;">Passport_Expiry</td>
                        <td>تاريخ انتهاء الجواز في الماضي. يجب أن يكون الجواز صالحاً 6 أشهر على الأقل من تاريخ السفر.</td>
                    </tr>
                    <tr>
                        <td><span class="pill pill-red">الصف 18</span></td>
                        <td dir="ltr" style="font-weight:600;">Nationality</td>
                        <td>الجنسية "XY" غير معرّفة في قائمة الجنسيات المتاحة. يرجى مراجعة قائمة الجنسيات المدعومة.</td>
                    </tr>
                    <tr>
                        <td><span class="pill pill-red">الصف 32</span></td>
                        <td dir="ltr" style="font-weight:600;">Transit_Stop_2_Country</td>
                        <td>عمود دولة العبور 2 مطلوب عند وجود محطة عبور 2. القيمة فارغة.</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <label class="reupload-zone" for="reupload-input">
        <div style="font-size:28px; color:#dc2626; margin-bottom:10px;"><i class="fas fa-arrow-up-from-bracket"></i></div>
        <div style="font-size:14px; font-weight:700; color:#b91c1c; margin-bottom:4px;">إعادة رفع الملف المصحح</div>
        <div style="font-size:12px; color:#dc2626;">انقر أو اسحب الملف المصحح هنا</div>
        <input type="file" id="reupload-input" accept=".xlsx,.csv" onchange="handleReupload(event)">
    </label>
</div>
```

- [ ] Verify all four states in browser using the demo buttons (Success / Error)
- [ ] Verify error table renders correctly with row numbers, column names, and Arabic error messages
- [ ] Verify re-upload zone appears only in error state

---

### Task 15: Commit Step 2

```bash
git add app/batch-inquiry.html
git commit -m "feat: add batch wizard Step 2 — upload & validate with all states

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Chunk 4: Steps 3 & 4 + JS Wizard Engine

### Task 16: Build Step 3 panel — Fee Review

**File:** `app/batch-inquiry.html` — `#panel-3` (modify)

- [ ] Replace the `<!-- Task 15 -->` comment with:

```html
<div class="step-heading">
    <h3><i class="fas fa-receipt" style="color:#1d4ed8;margin-left:8px;"></i>مراجعة الرسوم</h3>
    <p>تحقق من تفاصيل الرسوم لكل جنسية قبل تأكيد الخصم من المحفظة.</p>
</div>

<!-- Fee breakdown table -->
<div class="fee-table-card">
    <table class="fee-table">
        <thead>
            <tr>
                <th>الجنسية</th>
                <th>عدد المسافرين</th>
                <th>الرسوم للمسافر الواحد</th>
                <th>الإجمالي الفرعي</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><span class="pill pill-blue">أمريكي</span></td>
                <td>20</td>
                <td>250 جنيه</td>
                <td style="font-weight:700;">5,000 جنيه</td>
            </tr>
            <tr>
                <td><span class="pill pill-blue">بريطاني</span></td>
                <td>15</td>
                <td>200 جنيه</td>
                <td style="font-weight:700;">3,000 جنيه</td>
            </tr>
            <tr>
                <td><span class="pill pill-blue">كندي</span></td>
                <td>12</td>
                <td>200 جنيه</td>
                <td style="font-weight:700;">2,400 جنيه</td>
            </tr>
        </tbody>
        <tfoot>
            <tr class="fee-total-row">
                <td colspan="3" style="text-align:right;">الإجمالي الكلي (47 مسافراً)</td>
                <td id="grand-total">10,400 جنيه</td>
            </tr>
        </tfoot>
    </table>
</div>

<div class="non-refundable-note">
    <i class="fas fa-triangle-exclamation" style="color:#d97706;"></i>
    تنبيه: الرسوم غير قابلة للاسترداد بعد الإرسال
</div>

<!-- Wallet balance vs total -->
<div id="fee-wallet-ok">
    <div class="alert alert-success">
        <div class="alert-icon"><i class="fas fa-wallet"></i></div>
        <div class="alert-body">
            <strong>رصيد المحفظة كافٍ</strong>
            <span>رصيدك الحالي: <strong>12,500 جنيه</strong> — الرسوم المطلوبة: <strong>10,400 جنيه</strong> — الرصيد المتبقي بعد الخصم: <strong>2,100 جنيه</strong></span>
        </div>
    </div>
</div>

<div id="fee-wallet-insufficient" style="display:none;">
    <div class="alert alert-warning">
        <div class="alert-icon"><i class="fas fa-wallet"></i></div>
        <div class="alert-body">
            <strong>رصيد غير كافٍ</strong>
            <span>رصيدك الحالي: <strong>5,000 جنيه</strong> — الرسوم المطلوبة: <strong>10,400 جنيه</strong>. يرجى التواصل مع مسؤول النظام لشحن المحفظة.</span>
        </div>
    </div>
</div>

<!-- Demo toggle for insufficient balance -->
<div style="margin-top:12px; padding:10px 14px; background:#f8fafc; border:1px dashed #cbd5e1; border-radius:8px; font-size:12px; color:#64748b;">
    <strong>أدوات العرض التجريبي:</strong>
    <button onclick="toggleInsufficientBalance()" style="margin-right:8px; padding:4px 10px; font-size:11px; cursor:pointer; border:1px solid #cbd5e1; border-radius:4px; background:#fff;">
        تبديل: رصيد غير كافٍ
    </button>
</div>
```

- [ ] Verify fee table renders with nationalities, counts, fees, grand total
- [ ] Verify "sufficient balance" alert shows by default
- [ ] Verify demo button toggles to insufficient balance state and disables the Next button in footer

---

### Task 17: Build Step 4 panel — Confirm & Deduct

**File:** `app/batch-inquiry.html` — `#panel-4` (modify)

- [ ] Replace the `<!-- Task 16 -->` comment with:

```html
<!-- PRE-CONFIRM state -->
<div id="s4-confirm">
    <div class="step-heading">
        <h3><i class="fas fa-circle-check" style="color:#1d4ed8;margin-left:8px;"></i>تأكيد الخصم وإرسال الطلبات</h3>
        <p>راجع ملخص الدفعة قبل التأكيد النهائي. بعد التأكيد لن يمكن التراجع.</p>
    </div>

    <div class="confirm-summary-card">
        <div class="cs-row">
            <span class="cs-label">رقم مرجعي للدفعة</span>
            <span class="cs-value" dir="ltr">BTH-2026-0312</span>
        </div>
        <div class="cs-row">
            <span class="cs-label">إجمالي المسافرين</span>
            <span class="cs-value">47 مسافراً</span>
        </div>
        <div class="cs-row">
            <span class="cs-label">المبلغ المخصوم من المحفظة</span>
            <span class="cs-value total">10,400 جنيه</span>
        </div>
        <div class="cs-row">
            <span class="cs-label">رصيد المحفظة الحالي</span>
            <span class="cs-value">12,500 جنيه</span>
        </div>
        <div class="cs-row">
            <span class="cs-label">الرصيد المتبقي بعد الخصم</span>
            <span class="cs-value balance-after">2,100 جنيه</span>
        </div>
    </div>

    <div class="irreversible-warning">
        <i class="fas fa-triangle-exclamation"></i>
        بعد تأكيد الخصم، سيتم إرسال طلبات جميع المسافرين إلى محرك الفرز تلقائياً ولا يمكن التراجع.
    </div>

    <div style="display:flex; gap:12px; align-items:center;">
        <button class="btn btn-primary btn-lg" id="btn-confirm-deduct" onclick="simulateDeduction()">
            <i class="fas fa-circle-check"></i> تأكيد الخصم وإرسال الطلبات
        </button>
        <button class="btn btn-secondary btn-sm" onclick="goBack()">
            <i class="fas fa-arrow-right"></i> العودة لمراجعة الرسوم
        </button>
    </div>
</div>

<!-- LOADING state -->
<div id="s4-loading" style="display:none; text-align:center; padding:48px;">
    <div style="font-size:48px; color:#1d4ed8; margin-bottom:16px;">
        <i class="fas fa-spinner fa-spin"></i>
    </div>
    <h4 style="font-size:16px; font-weight:700; color:#0f172a; margin-bottom:6px;">جارٍ معالجة الطلب…</h4>
    <p style="font-size:13px; color:#64748b;">يُرجى الانتظار. يتم خصم الرسوم وإرسال طلبات المسافرين.</p>
</div>

<!-- SUCCESS state -->
<div id="s4-success" style="display:none;">
    <div class="success-screen">
        <div class="success-icon-wrap"><i class="fas fa-circle-check"></i></div>
        <h3>تم الإرسال بنجاح!</h3>
        <p>تم خصم الرسوم وإرسال جميع طلبات المسافرين إلى محرك الفرز</p>

        <div class="success-detail-card">
            <div class="sd-row">
                <span class="sd-label">رقم مرجعي للدفعة</span>
                <span class="sd-value" dir="ltr">BTH-2026-0312</span>
            </div>
            <div class="sd-row">
                <span class="sd-label">إجمالي المسافرين المُرسَلين</span>
                <span class="sd-value">47 مسافراً</span>
            </div>
            <div class="sd-row">
                <span class="sd-label">المبلغ المخصوم</span>
                <span class="sd-value" style="color:#1d4ed8;">10,400 جنيه</span>
            </div>
            <div class="sd-row">
                <span class="sd-label">الرصيد المتبقي في المحفظة</span>
                <span class="sd-value" style="color:#16a34a;">2,100 جنيه</span>
            </div>
            <div class="sd-row">
                <span class="sd-label">التوقيت</span>
                <span class="sd-value" dir="ltr">16 مارس 2026 — 14:32</span>
            </div>
        </div>

        <div class="success-actions">
            <a href="#" class="btn btn-primary">
                <i class="fas fa-layer-group"></i> عرض تفاصيل الدفعة
            </a>
            <button class="btn btn-secondary" onclick="alert('سيتم تحميل إيصال PDF من الخادم')">
                <i class="fas fa-file-arrow-down"></i> تحميل إيصال الدفعة
            </button>
        </div>

        <a href="batch-inquiry.html" class="success-new-link">
            <i class="fas fa-plus"></i> إرسال دفعة جديدة
        </a>
    </div>
</div>

<!-- SYSTEM ERROR state -->
<div id="s4-error" style="display:none;">
    <div class="error-screen">
        <div class="error-icon-wrap"><i class="fas fa-circle-xmark"></i></div>
        <h3 style="font-size:20px; font-weight:800; color:#0f172a; margin-bottom:8px;">حدث خطأ في المعالجة</h3>
        <p style="font-size:13px; color:#64748b; margin-bottom:16px;">
            تم خصم الرسوم، لكن حدث خطأ أثناء إنشاء الدفعة. الرجاء التواصل مع الدعم الفني مع الإشارة إلى الرقم المرجعي أدناه.
        </p>
        <div class="error-ref">REF: ERR-2026-0316-8821</div>
        <div style="display:flex; gap:10px; justify-content:center;">
            <a href="#" class="btn btn-danger">
                <i class="fas fa-headset"></i> التواصل مع الدعم
            </a>
            <button class="btn btn-secondary" onclick="resetToStep4Confirm()">
                إعادة المحاولة
            </button>
        </div>
        <!-- Demo button -->
        <div style="margin-top:20px; font-size:11px; color:#94a3b8;">
            <button onclick="showS4Success()" style="font-size:11px; color:#94a3b8; background:none; border:none; cursor:pointer; text-decoration:underline;">← عرض حالة النجاح</button>
        </div>
    </div>
</div>
```

- [ ] Verify pre-confirm summary card renders with all rows
- [ ] Verify irreversible warning renders in amber
- [ ] Demo: clicking "تأكيد الخصم" shows loading spinner for 2 seconds then transitions to success screen

---

### Task 18: Write the JS wizard engine

**File:** `app/batch-inquiry.html` — `<script>` block (modify)

- [ ] Replace the `// Wizard state — populated in Task 17` comment with the full wizard JS:

```javascript
// ── State ────────────────────────────────────────────────────────────────────
let currentStep  = 1;
const maxStep    = 4;
const completed  = new Set();   // steps that have been completed
let s2State      = 'upload';    // 'upload' | 'loading' | 'success' | 'error'
let s4State      = 'confirm';   // 'confirm' | 'loading' | 'success' | 'error'
let insufficientBalance = false;
let selectedFile = null;

// ── Stepper rendering ────────────────────────────────────────────────────────
function renderStepper() {
    for (let i = 1; i <= maxStep; i++) {
        const node   = document.getElementById('node-' + i);
        const circle = node.querySelector('.step-circle');

        node.className = 'step-node';  // reset

        if (completed.has(i) && i !== currentStep) {
            node.classList.add('completed');
            circle.innerHTML = '<i class="fas fa-check"></i>';
        } else if (i === currentStep) {
            node.classList.add('active');
            circle.textContent = i;
        } else {
            node.classList.add('locked');
            circle.innerHTML = '<i class="fas fa-lock"></i>';
        }

        if (i < maxStep) {
            const line = document.getElementById('line-' + i + '-' + (i + 1));
            line.className = 'step-line' +
                (completed.has(i) ? ' completed' : (i === currentStep ? ' active' : ''));
        }
    }

    // Back button
    const btnBack = document.getElementById('btn-back');
    btnBack.style.visibility = currentStep === 1 ? 'hidden' : 'visible';

    // Next button
    updateNextButton();
}

// ── Step panels ──────────────────────────────────────────────────────────────
function showPanel(step) {
    for (let i = 1; i <= maxStep; i++) {
        document.getElementById('panel-' + i).classList.remove('active');
    }
    document.getElementById('panel-' + step).classList.add('active');
}

// ── Navigation ───────────────────────────────────────────────────────────────
function goToStep(step) {
    if (step === currentStep) return;
    if (!completed.has(step) && step > currentStep) return;  // locked
    if (step < 1 || step > maxStep) return;
    currentStep = step;
    showPanel(step);
    renderStepper();
    window.scrollTo({ top: 0, behavior: 'smooth' });
}

function goNext() {
    if (!canProceed()) return;
    completed.add(currentStep);
    if (currentStep < maxStep) {
        currentStep++;
        showPanel(currentStep);
        renderStepper();
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }
}

function goBack() {
    if (currentStep > 1) {
        currentStep--;
        showPanel(currentStep);
        renderStepper();
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }
}

function cancelWizard() {
    if (confirm('هل تريد إلغاء هذا الطلب والعودة للرئيسية؟')) {
        window.location.href = 'index.html';
    }
}

// ── Next-button state ────────────────────────────────────────────────────────
function canProceed() {
    if (currentStep === 1) return true;
    if (currentStep === 2) return s2State === 'success';
    if (currentStep === 3) return !insufficientBalance;
    return false; // step 4 uses its own confirm button
}

function updateNextButton() {
    const btn = document.getElementById('btn-next');
    // Hide next button on step 4 (step 4 uses its own confirm button)
    if (currentStep === maxStep) {
        btn.style.display = 'none';
        return;
    }
    btn.style.display = '';
    const ok = canProceed();
    btn.disabled = !ok;
    btn.style.opacity = ok ? '1' : '.45';
    btn.style.cursor  = ok ? 'pointer' : 'not-allowed';
}

// ── Step 1: Download template ────────────────────────────────────────────────
function toggleColumns() {
    const wrap = document.getElementById('columns-table-wrap');
    const icon = document.getElementById('col-toggle-icon');
    if (wrap.classList.contains('hidden')) {
        wrap.classList.remove('hidden');
        icon.className = 'fas fa-chevron-up';
    } else {
        wrap.classList.add('hidden');
        icon.className = 'fas fa-chevron-down';
    }
}

// ── Step 2: Upload & validate ────────────────────────────────────────────────
function handleFileSelect(event) {
    const file = event.target.files[0];
    if (!file) return;
    selectedFile = file;
    document.getElementById('selected-file-name').textContent = '📎 ' + file.name;
    document.getElementById('btn-upload').disabled = false;
}

function handleDragOver(e) {
    e.preventDefault();
    document.getElementById('dropzone').classList.add('drag-over');
}

function handleDragLeave() {
    document.getElementById('dropzone').classList.remove('drag-over');
}

function handleDrop(e) {
    e.preventDefault();
    handleDragLeave();
    const file = e.dataTransfer.files[0];
    if (file) {
        selectedFile = file;
        document.getElementById('selected-file-name').textContent = '📎 ' + file.name;
        document.getElementById('btn-upload').disabled = false;
    }
}

function simulateValidation() {
    setS2State('loading');
    setTimeout(() => {
        // Default: show success
        showS2Success();
    }, 1800);
}

function setS2State(state) {
    s2State = state;
    ['upload','loading','success','error'].forEach(s => {
        document.getElementById('s2-' + s).style.display = s === state ? '' : 'none';
    });
    updateNextButton();
}

function showS2Success() { setS2State('success'); }
function showS2Error()   { setS2State('error');   }

function resetUpload() {
    selectedFile = null;
    document.getElementById('file-input').value = '';
    document.getElementById('selected-file-name').textContent = '';
    document.getElementById('btn-upload').disabled = true;
    setS2State('upload');
}

function handleReupload(event) {
    const file = event.target.files[0];
    if (!file) return;
    selectedFile = file;
    setS2State('loading');
    setTimeout(() => showS2Success(), 1800);
}

// ── Step 3: Fee review ───────────────────────────────────────────────────────
function toggleInsufficientBalance() {
    insufficientBalance = !insufficientBalance;
    document.getElementById('fee-wallet-ok').style.display          = insufficientBalance ? 'none' : '';
    document.getElementById('fee-wallet-insufficient').style.display = insufficientBalance ? '' : 'none';
    updateNextButton();
}

// ── Step 4: Confirm & deduct ─────────────────────────────────────────────────
function simulateDeduction() {
    setS4State('loading');
    setTimeout(() => showS4Success(), 2000);
}

function setS4State(state) {
    s4State = state;
    ['confirm','loading','success','error'].forEach(s => {
        document.getElementById('s4-' + s).style.display = s === state ? '' : 'none';
    });
    // Hide footer on success/error
    document.querySelector('.step-footer').style.display =
        (state === 'success' || state === 'error') ? 'none' : '';
}

function showS4Success() { setS4State('success'); }
function resetToStep4Confirm() { setS4State('confirm'); }

// ── Notification & user dropdowns (same as dashboard) ─────────────────────
function toggleNotif() {
    document.getElementById('notifDrawer').classList.toggle('open');
    document.getElementById('userDropdown').classList.remove('open');
}
function toggleUser() {
    document.getElementById('userDropdown').classList.toggle('open');
    document.getElementById('notifDrawer').classList.remove('open');
}
function markAllRead() {
    document.getElementById('notifDot').style.display = 'none';
    document.getElementById('notifDrawer').classList.remove('open');
}
function setLang(btn) {
    document.querySelectorAll('.lang-toggle button').forEach(b => b.classList.remove('active'));
    btn.classList.add('active');
}
document.addEventListener('click', (e) => {
    if (!e.target.closest('#bellBtn') && !e.target.closest('#notifDrawer'))
        document.getElementById('notifDrawer')?.classList.remove('open');
    if (!e.target.closest('#userChip') && !e.target.closest('#userDropdown'))
        document.getElementById('userDropdown')?.classList.remove('open');
});

// ── Init ─────────────────────────────────────────────────────────────────────
renderStepper();
```

- [ ] Open in browser and test the full flow: Step 1 → Next → Step 2 upload demo (success) → Next → Step 3 fee review → Next → Step 4 confirm → click confirm → loading → success screen
- [ ] Test: go back, completed steps navigable by click, locked steps not navigable
- [ ] Test: Step 3 insufficient balance toggle disables Next button
- [ ] Test: demo error state in Step 2, verify re-upload zone is shown and Next stays locked

---

### Task 19: Commit Steps 3, 4, and JS engine

```bash
git add app/batch-inquiry.html
git commit -m "feat: add batch wizard Steps 3+4 and JS wizard engine

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Chunk 5: Excel Template

### Task 20: Write `tools/scripts/generate-batch-template.py`

**File:** `tools/scripts/generate-batch-template.py` (create at project root)

- [ ] Check if openpyxl is available:

```bash
python -c "import openpyxl; print('openpyxl OK:', openpyxl.__version__)"
```

Expected: `openpyxl OK: <version>`. If not found:

```bash
pip install openpyxl
```

- [ ] Write the script:

```python
from openpyxl import Workbook
from openpyxl.styles import (
    Font, PatternFill, Alignment, Border, Side, Protection
)
from openpyxl.utils import get_column_letter
import os

wb = Workbook()
ws = wb.active
ws.title = "Batch Template"
ws.sheet_view.rightToLeft = False  # LTR for data entry

# ── Column definitions ─────────────────────────────────────────────────────
COLUMNS = [
    ("Traveler_Name",           "Required",    "Full name as in passport (2–150 chars)"),
    ("Nationality",             "Required",    "Nationality — must match configured list"),
    ("Passport_Number",         "Required",    "6–20 alphanumeric characters"),
    ("Passport_Expiry",         "Required",    "YYYY-MM-DD — must be 6+ months from Travel_Date"),
    ("Travel_Date",             "Required",    "YYYY-MM-DD — today or future"),
    ("Departure_Country",       "Required",    "Country of departure"),
    ("Arrival_Airport_Egypt",   "Required",    "Select from configured Egyptian airport list"),
    ("Purpose",                 "Required",    "Tourism / Business / Medical / Family / Education / Conference / Transit / Other"),
    ("Flight_Number",           "Optional",    "Max 10 alphanumeric chars"),
    ("Transit_Stop_1_Country",  "Conditional", "Required if stop 1 exists"),
    ("Transit_Stop_1_Airport",  "Optional",    "Airport code or name for stop 1"),
    ("Transit_Stop_1_Hours",    "Optional",    "Layover duration 0–72 hours"),
    ("Transit_Stop_1_Flight",   "Optional",    "Connecting flight number for stop 1"),
    ("Transit_Stop_2_Country",  "Conditional", "Required if stop 2 exists"),
    ("Transit_Stop_2_Airport",  "Optional",    "Airport code or name for stop 2"),
    ("Transit_Stop_2_Hours",    "Optional",    "Layover duration 0–72 hours"),
    ("Transit_Stop_2_Flight",   "Optional",    "Connecting flight number for stop 2"),
    ("Transit_Stop_3_Country",  "Conditional", "Required if stop 3 exists"),
    ("Transit_Stop_3_Airport",  "Optional",    "Airport code or name for stop 3"),
    ("Transit_Stop_3_Hours",    "Optional",    "Layover duration 0–72 hours"),
    ("Transit_Stop_3_Flight",   "Optional",    "Connecting flight number for stop 3"),
]

# ── Styles ─────────────────────────────────────────────────────────────────
header_fill    = PatternFill("solid", fgColor="1D4ED8")   # $color-primary
guidance_fill  = PatternFill("solid", fgColor="EFF6FF")   # $color-primary-light
req_fill       = PatternFill("solid", fgColor="FEE2E2")   # danger light
cond_fill      = PatternFill("solid", fgColor="FEF3C7")   # warning light
opt_fill       = PatternFill("solid", fgColor="F8FAFC")   # bg-alt

header_font    = Font(name="Calibri", bold=True, color="FFFFFF", size=11)
guidance_font  = Font(name="Calibri", italic=True, color="1E40AF", size=10)
data_font      = Font(name="Calibri", size=11)

center_align   = Alignment(horizontal="center", vertical="center", wrap_text=True)
left_align     = Alignment(horizontal="left",   vertical="center", wrap_text=True)

thin_side      = Side(style="thin", color="CBD5E1")
thin_border    = Border(left=thin_side, right=thin_side, top=thin_side, bottom=thin_side)

# ── Row 1: Headers ──────────────────────────────────────────────────────────
ws.row_dimensions[1].height = 28
for col_idx, (col_name, req, _) in enumerate(COLUMNS, start=1):
    cell = ws.cell(row=1, column=col_idx, value=col_name)
    cell.font      = header_font
    cell.fill      = header_fill
    cell.alignment = center_align
    cell.border    = thin_border

# ── Row 2: Guidance notes ───────────────────────────────────────────────────
ws.row_dimensions[2].height = 48
for col_idx, (_, req, guidance) in enumerate(COLUMNS, start=1):
    fill = req_fill if req == "Required" else (cond_fill if req == "Conditional" else opt_fill)
    cell = ws.cell(row=2, column=col_idx, value=f"[{req}] {guidance}")
    cell.font      = guidance_font
    cell.fill      = fill
    cell.alignment = Alignment(horizontal="left", vertical="center", wrap_text=True)
    cell.border    = thin_border

# ── Rows 3–52: Data entry rows (50 rows) ────────────────────────────────────
for row in range(3, 53):
    ws.row_dimensions[row].height = 20
    for col_idx in range(1, len(COLUMNS) + 1):
        cell = ws.cell(row=row, column=col_idx)
        cell.font      = data_font
        cell.alignment = left_align
        cell.border    = thin_border

# ── Column widths ───────────────────────────────────────────────────────────
COL_WIDTHS = [
    22, 18, 20, 18, 14, 22, 26, 14, 14,
    22, 22, 14, 18,
    22, 22, 14, 18,
    22, 22, 14, 18,
]
for i, width in enumerate(COL_WIDTHS, start=1):
    ws.column_dimensions[get_column_letter(i)].width = width

# ── Freeze top two rows ─────────────────────────────────────────────────────
ws.freeze_panes = "A3"

# ── Sheet tab colour ────────────────────────────────────────────────────────
ws.sheet_properties.tabColor = "1D4ED8"

# ── Save ────────────────────────────────────────────────────────────────────
output_dir = os.path.join(os.path.dirname(__file__), "frontend", "assets")
os.makedirs(output_dir, exist_ok=True)
output_path = os.path.join(output_dir, "batch-template.xlsx")
wb.save(output_path)
print(f"✓ Template saved to: {output_path}")
print(f"  Columns: {len(COLUMNS)}")
print(f"  Data rows: 50 (rows 3–52)")
```

---

### Task 21: Run the script and verify the template

- [ ] Run the script:

```bash
python tools/scripts/generate-batch-template.py
```

Expected output:
```
✓ Template saved to: .../assets/batch-template.xlsx
  Columns: 21
  Data rows: 50 (rows 3–52)
```

- [ ] Open `app/assets/batch-template.xlsx` in Excel or LibreOffice
- [ ] Verify: Row 1 has blue header fill with white bold text, all 21 column names present
- [ ] Verify: Row 2 has guidance notes with color-coded fills (red = required, amber = conditional, gray = optional)
- [ ] Verify: Top 2 rows are frozen (scrolling data rows doesn't move headers)
- [ ] Verify: Column widths are readable

---

### Task 22: Generate CSV counterpart

- [ ] Add after the xlsx save in `tools/scripts/generate-batch-template.py`:

```python
import csv

csv_path = os.path.join(output_dir, "batch-template.csv")
with open(csv_path, "w", newline="", encoding="utf-8-sig") as f:
    writer = csv.writer(f)
    writer.writerow([col[0] for col in COLUMNS])
    writer.writerow([f"[{col[1]}] {col[2]}" for col in COLUMNS])
print(f"✓ CSV saved to: {csv_path}")
```

- [ ] Re-run script, verify `batch-template.csv` is created with headers + guidance row

---

### Task 23: Commit template files

```bash
git add tools/scripts/generate-batch-template.py app/assets/
git commit -m "feat: add 21-column batch inquiry Excel/CSV template with guidance row

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Chunk 6: Final Review

### Task 24: End-to-end walkthrough

- [ ] Open `app/batch-inquiry.html` in browser
- [ ] Walk through the full wizard flow:
  1. Step 1: Toggle column table open/closed. Click xlsx download button.
  2. Step 2: Click "نجاح التحقق" demo button → verify success state → Next unlocks
  3. Step 3: Verify fee table renders. Toggle "رصيد غير كافٍ" → verify Next disables. Toggle back → Next re-enables.
  4. Step 4: Click "تأكيد الخصم" → loading spinner → success screen.
- [ ] Test error flow: Step 2 "أخطاء في التحقق" → verify error table + re-upload zone, Next stays locked
- [ ] Test navigation: after completing step 2, click step 1 circle in stepper → goes back read-only
- [ ] Open `app/index.html` → verify no visual change after CSS refactor

### Task 25: Final commit

```bash
git add .
git commit -m "feat: batch inquiry wizard — complete implementation

- Shared SCSS design system (styles/)
- app/index.html and app/registration.html refactored to main.css
- batch-inquiry.html: 4-step wizard with all states
- Excel + CSV batch template (21 columns, 50 data rows)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```


